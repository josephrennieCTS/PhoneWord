namespace Phoneword
{
    public interface IDialer
    {
        bool Dial(string number);
    }
}
//this page is what allows the phone to dial a number when the call button is pressed and the messaged has been agreed to when yes is pressed.